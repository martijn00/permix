import {
  extractPermissions,
  generatePermissions,
  validatePermissionCoverage,
} from 'permix/extractor'

export const extractor = {
  extractPermissions,
  generatePermissions,
  validatePermissionCoverage,
}
